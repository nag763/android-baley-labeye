<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ticandroid.baley_labeye">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Baley_Labeye">

        <activity android:name=".activities.RouteMapActivity" />
        <activity android:name=".activities.MuseumReaderActivity" />
        <activity android:name=".activities.MuseumListActivity"/>
        <activity
            android:name=".activities.StartActivity"
            android:label="@string/title_activity_start"
            android:theme="@style/Theme.Baley_Labeye.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.ProfilActivity" />
        <activity android:name=".activities.ConnexionActivity" />
        <activity android:name=".activities.InscriptionActivity" />

        <activity android:name=".activities.MainActivity2"
            android:theme="@style/Theme.Baley_Labeye.NoActionBar">

        </activity>

    </application>

</manifest>
